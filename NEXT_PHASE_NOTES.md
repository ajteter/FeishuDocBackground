# 当前阶段结论

## 已完成功能

- 已支持浏览器浅色 / 深色模式自动跟随。
- 已支持手动固定浅色或暗色配置。
- 已支持两套独立颜色配置：
  - 浅色模式颜色
  - 暗色模式颜色
- 已支持以下飞书页面：
  - `docx` 文档页
  - `wiki` 普通文档页
  - `sheets` 表格页
  - `wiki` 中由表格引擎承载的表格页

## 表格页方案结论

- 当前表格页只修改默认底色。
- 不修改自定义单元格填充色。
- 不修改文字、边框、选区高亮等内容层。
- 这是一条保守方案，目标是：
  - 不破坏飞书表格渲染
  - 不扫描所有单元格
  - 不改 canvas 像素
  - 尽量保持加载性能

## 已确认的问题与修复

### 1. 表格内容消失问题

现象：

- 页面刷新后先看到背景色生效。
- 随后文字和表格内容消失，只剩背景。

根因：

- 之前的样式命中了飞书表格的浮层或渲染层。
- 这些层位于真实表格内容的上方，导致背景色把网格和文本一起盖住。

修复结论：

- 只能给表格底层容器上色。
- 不能给以下层直接上色：
  - canvas 渲染层
  - 浮层
  - 交互覆盖层

当前处理方式：

- 只保留底层容器背景改色。
- 明确排除 overlay / canvas 相关层。

结果：

- 表格背景可以正常替换。
- 表格内容、文字、网格线保持可见。

## 性能与效率结论

### 1. 当前方案总体是轻量的

当前方案没有做这些高成本操作：

- 没有全量 DOM 扫描
- 没有逐单元格改色
- 没有 canvas 像素级重绘
- 没有后台脚本持续轮询

因此从整体设计上看，这版已经是偏轻量的实现。

### 2. 已做过的性能收紧

已经收紧了两个点：

- `wiki` 表格识别观察逻辑只在 `suite-sheet` 状态真的切换时才响应，不再因为任意 `body.class` 变化反复触发。
- 表格页不再复用文档页的 `body::before` 全屏背景层，减少一层非必要绘制。

结论：

- 当前加载速度已经比较快。
- 体感上的主要问题不再是脚本运行慢，而是首屏颜色切换时序。

## 当前仍可见的“闪一下”结论

现象：

- 页面加载时，先看到飞书原始背景色。
- 零点几秒后，切换到插件设置的背景色。

根因：

- `content.css` 可以很早注入。
- 但真实颜色值依赖 `chrome.storage.local` 里的用户设置。
- 这些设置需要由 `content.js` 异步读取后，才能写入页面变量和标记。
- 在这之前，飞书自己的默认背景已经先渲染出来。

结论：

- 这个闪烁不是因为“表格处理太重”。
- 本质上是“样式注入早于用户配置读取”带来的首屏时序问题。

## 关于“还能不能更快”的结论

可以更快，但收益已经开始变小。

### 目前方案的判断

- 当前速度已经接近“支持用户自定义颜色 + 保持实现轻量”这条路线的上限。
- 可以继续优化首屏闪烁，但属于边际收益较小的工作。

### 可选方向

#### 方向 A：继续保留任意自定义颜色

优点：

- 用户自由度最高

缺点：

- 首屏几乎无法做到完全无闪
- 因为必须先读取用户存储的颜色

#### 方向 B：使用少量预设颜色

优点：

- 比任意自定义更容易提前命中样式
- 有机会进一步减少闪烁

缺点：

- 灵活性下降
- 仍然可能需要知道用户当前选的是哪套预设

#### 方向 C：极致优化，改为固定内置颜色

优点：

- 最接近无闪
- CSS 可在页面最早阶段直接命中
- 可以直接结合 `prefers-color-scheme`

缺点：

- 用户不能任意改色
- 只能使用内置浅色 / 暗色方案

## 对“极致优化”的准确理解

“极致优化”不是单纯把代码再写快一点，而是牺牲颜色自定义能力，换取更早的首屏命中。

最极端形式是：

- 浅色固定一个内置颜色
- 暗色固定一个内置颜色
- 跟随系统主题直接通过 CSS 切换
- 不再依赖运行时读取用户自定义颜色

这样最接近无闪。

## 下期可继续讨论的方向

下期如果继续推进，建议优先从下面两条路线里二选一：

### 路线 1：保留自定义颜色

目标：

- 接受少量首屏闪烁
- 继续保持用户可以自由选色

可研究内容：

- 是否能做“上次颜色预热”
- 是否能进一步缩小表格页命中的容器范围

### 路线 2：优先首屏体验

目标：

- 尽量接近无闪
- 提升首次渲染观感

代价：

- 需要减少甚至取消任意自定义颜色能力
- 改为固定内置色或非常有限的预设色

## 当前建议

- 以当前版本作为本期收口是合理的。
- 主题跟随已经完成。
- 表格页默认底色支持已经可用。
- 性能已经收紧到一个比较稳妥的状态。
- 如果下期继续做，建议优先先定产品取舍：
  - 要自定义颜色自由度
  - 还是要几乎无闪的首屏体验
